# Agent 17: Frontend API Client

## 📌 Context
백엔드 API를 호출하기 위한 클라이언트 코드(Fetcher)를 작성하고, 데이터 타입을 정의합니다.

## ✅ Checklist
- [ ] `ApiClient` 설정 (Axios 인스턴스, 인터셉터 등)
- [ ] API 엔드포인트별 Fetcher 함수 구현 (`services/api/*.ts`)
- [ ] React Query (TanStack Query) 훅 작성 (`useRanking`, `useReplay` 등)
- [ ] 백엔드 DTO 타입 정의 (`types/api.d.ts`)

## 💬 Communication
- **Status**: [Pending]
- **Current Issue**: 
- **Memo**: 백엔드 에이전트(Agent 12)가 만든 명세서를 적극 참고하세요.

## 🚀 Prompts

### 시작 프롬프트
```markdown
당신은 프론트엔드 아키텍트입니다.
백엔드 API와 통신하기 위한 **API 클라이언트 레이어**를 구성해야 합니다.

1. `fetch` 또는 `axios`를 래핑하여 기본 URL 및 에러 핸들링이 포함된 인스턴스를 만드세요.
2. `TanStack Query`를 사용하여 데이터를 캐싱하고 상태를 관리하는 커스텀 훅 패턴을 정의하세요.
```

### 이어지는 프롬프트
```markdown
명예의 전당(`Hall of Fame`) 관련 API 훅을 구현해주세요.
- `useGetHistory(page, limit)`
- `useGetGeneralRanking(filter)`
- `useGetNationRanking(filter)`
각 훅은 로딩 상태, 에러 상태, 데이터를 반환해야 합니다. 타입스크립트 제네릭을 활용해 응답 타입을 명시해주세요.
```






