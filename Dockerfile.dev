# 개발용 Dockerfile - 핫 리로드 지원
FROM node:20-alpine

WORKDIR /app

# 의존성 설치
COPY package*.json ./
RUN npm ci --legacy-peer-deps

# 소스는 볼륨 마운트로 제공됨
# Next.js가 파일 변경 감지

EXPOSE 3000

# 개발 서버 실행
CMD ["npm", "run", "dev"]
